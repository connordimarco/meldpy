numpy>=1.24
pandas>=2.0,<3
matplotlib>=3.3
requests>=2.24
cdflib>=1.3
netCDF4>=1.7
spacepy>=0.7
# Optional: only required for downloading raw data (l1_downloaders). Not
# needed for processing from an existing L1_raw/ tree.
# pyspedas>=2.0
